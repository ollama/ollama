# Security Audit — 2026-01-27

Summary:

- Tool: `pip-audit` (virtualenv `.venv-dev`)
- Findings: 1 known vulnerability detected

Details:

- Package: `protobuf` (installed version: 4.25.8)
- Advisory: `CVE-2026-0994` / GHSA-7gcm-g887-7qv7
- Description: Denial-of-service via bypassing recursion depth checks when parsing nested `google.protobuf.Any` messages using `google.protobuf.json_format.ParseDict()`.
- Fix status: No fixed version reported by `pip-audit` database at time of scan.

Immediate Mitigation Plan:

1. Avoid parsing untrusted JSON into protobuf `Any` messages where possible. Replace uses of `ParseDict` on untrusted inputs with safe parsing or validation layers.
2. Monitor upstream `protobuf` advisories for a patched release. When a fixed `protobuf` version is available, pin it in `pyproject.toml` and update the environment:

   ```bash
   # in dev venv
   .venv-dev/bin/pip install --upgrade protobuf==<fixed-version>
   git add pyproject.toml
   git commit -m "chore(deps): pin protobuf to patched version <fixed-version>"
   git push
   ```

3. Add `pip-audit` to CI and schedule daily/weekly dependency scans (via `cron` or GitHub Actions) to catch future advisories.

4. Record this finding in the PMO remediation engine for tracking and automated remediation when a fix is available.

Next steps (assigned):

- Create an issue to track `CVE-2026-0994` remediation and assign to `security` team.
- Implement code-level mitigations for any repository usages of `ParseDict` (none found during scan).
- Re-run `pip-audit` after upstream patch/release.

Generated by automated audit run on 2026-01-27.
