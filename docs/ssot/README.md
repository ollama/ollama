# SSOT Alias

The canonical documentation map lives in [docs/meta/README.md](../meta/README.md).

This folder exists as a naming compatibility alias so the repository can keep a clear `ssot` path while still maintaining one canonical content source.
