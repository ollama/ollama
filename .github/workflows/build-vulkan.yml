name: Build Ollama Vulkan Only

on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Maximize Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo docker system prune -af --volumes
          df -h

      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: ollama/ollama
          ref: main

      - name: Modify Dockerfile
        run: |
          cat > modify.py << 'EOF'
          with open('Dockerfile', 'r') as f:
              lines = f.readlines()
          
          output = []
          i = 0
          skip = False
          in_cpu = False
          
          while i < len(lines):
              line = lines[i]
              
              # 替换 base-amd64，添加重试和容错
              if 'FROM --platform=linux/amd64 rocm/dev-almalinux' in line and 'AS base-amd64' in line:
                  output.append('FROM --platform=linux/amd64 ubuntu:24.04 AS base-amd64\n')
                  i += 1
                  while i < len(lines) and not lines[i].startswith('FROM'):
                      i += 1
                  # 添加容错的 apt 安装
                  output.append('RUN for i in 1 2 3; do \\\n')
                  output.append('        apt-get update --allow-releaseinfo-change -o Acquire::Check-Valid-Until=false && break || sleep 5; \\\n')
                  output.append('    done && \\\n')
                  output.append('    apt-get install -y --no-install-recommends \\\n')
                  output.append('        git build-essential gcc g++ curl wget ccache ca-certificates \\\n')
                  output.append('        gnupg software-properties-common rsync python3 ninja-build pkg-config && \\\n')
                  output.append('    wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | tee /etc/apt/trusted.gpg.d/lunarg.asc && \\\n')
                  output.append('    wget -qO /etc/apt/sources.list.d/lunarg-vulkan-noble.list https://packages.lunarg.com/vulkan/lunarg-vulkan-noble.list && \\\n')
                  output.append('    for i in 1 2 3; do \\\n')
                  output.append('        apt-get update && apt-get install -y vulkan-sdk && break || sleep 5; \\\n')
                  output.append('    done && \\\n')
                  output.append('    rm -rf /var/lib/apt/lists/*\n')
                  continue
              
              if line.startswith('FROM') and 'AS cpu' in line:
                  in_cpu = True
              elif in_cpu and line.startswith('FROM'):
                  in_cpu = False
              
              if in_cpu and (line.startswith('RUN dnf') or ('ENV PATH=' in line and 'gcc-toolset' in line)):
                  i += 1
                  continue
              
              if 'FROM' in line and (('nvidia/cuda' in line and 'AS cuda-' in line) or 'AS rocm-6' in line or ('jetpack' in line)):
                  stage = line.split('AS')[1].strip()
                  output.append(f'FROM scratch AS {stage}\n')
                  skip = True
                  i += 1
                  continue
              
              if skip:
                  if line.startswith('FROM'):
                      skip = False
                  else:
                      i += 1
                      continue
              
              if line.startswith('COPY --from=cuda-') or line.startswith('COPY --from=jetpack-') or line.startswith('COPY --from=rocm-6'):
                  output.append('# ' + line)
                  i += 1
                  continue
              
              output.append(line)
              i += 1
          
          with open('Dockerfile', 'w') as f:
              f.writelines(output)
          EOF
          
          python3 modify.py

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build
        uses: docker/build-push-action@v5
        timeout-minutes: 240
        with:
          context: .
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository_owner }}/ollama-vulkan:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
